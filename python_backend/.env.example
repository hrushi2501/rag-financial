# ============================================================================
# Financial RAG Chatbot - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual API keys and preferences

# ----------------------------------------------------------------------------
# VECTOR DATABASE (Pinecone) - REQUIRED for vector search
# ----------------------------------------------------------------------------
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_INDEX_NAME=financial-rag
PINECONE_ENVIRONMENT=us-east-1

# ----------------------------------------------------------------------------
# LLM (Google Gemini) - REQUIRED for intelligent answer generation
# ----------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here

# ----------------------------------------------------------------------------
# EMBEDDING MODEL - Uses FinBERT by default for financial domain
# ----------------------------------------------------------------------------
# Model from HuggingFace (must produce 768-dim embeddings)
EMBEDDING_MODEL=ProsusAI/finbert

# ----------------------------------------------------------------------------
# TEXT PROCESSING - Adjust chunk size for your documents
# ----------------------------------------------------------------------------
# Characters per chunk (larger = more context, but slower)
CHUNK_SIZE=500

# Overlap between chunks (helps maintain context across boundaries)
CHUNK_OVERLAP=50

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION - Flask settings
# ----------------------------------------------------------------------------
# Port for Flask server
PORT=5000

# Enable debug mode for development (auto-reload on code changes)
# WARNING: Do not enable in production
FLASK_DEBUG=0

# ----------------------------------------------------------------------------
# FILE UPLOAD LIMITS - Security and performance
# ----------------------------------------------------------------------------
# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Maximum number of files per upload request
MAX_FILES_PER_REQUEST=10

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. Get Pinecone API key from: https://www.pinecone.io/
# 2. Get Gemini API key from: https://makersuite.google.com/app/apikey
# 3. FinBERT is optimized for financial text analysis
# 4. All keys must be kept secret - never commit .env to version control
